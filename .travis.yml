language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-yo-gulp-coffee-compass' ]; then cd .. && eval "mv $currentfolder generator-yo-gulp-coffee-compass" && cd generator-yo-gulp-coffee-compass; fi
